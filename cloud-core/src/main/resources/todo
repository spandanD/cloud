Backend
	Features
		1. Channels
		2. Broadcaster
		3. CloudService, with URL redirect
		4. Dependency injection with Guice
		5. Annotations like REST CHAT
		6. MIME type
		7. Write any content to suspended connections
		8. Private room (subset of suspended connections)
		9. Overflow considerations in queue
		10. EventListener
		
		 
Frontend
		1. (DONE) CSS for UI
		2. (DONE) Username prompt
		3. (DONE) Connection information
	
	
Performance
		1. (DONE) Use thread pool to broadcast to suspended connections
		2. Jmeter profiling
		
		
Priority
		1. Move web service to framework
		2. UI
		3. DI (like broadcaster)
